"""
Roles repository.
TODO(session-5): Implement per COPILOT_GUIDE.md Session 5.
Functions needed:
  list_roles(db) -> List[Role with permissions eagerly loaded]
  get_role_by_id(db, role_id) -> Role | None
  list_permissions(db) -> List[Permission]
  assign_permissions(db, role_id, grant_ids, revoke_ids) -> Role
"""
